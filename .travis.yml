language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# CI server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

# CI mail 알림
notifications:
  email:
    recipients:
      - 'soccer_by@naver.com'