
server:
  port: 8083
spring :
  config:
    activate:
      on-profile: release
#  profiles:
##    active: release
#    include: release
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type: info