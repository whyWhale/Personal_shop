server:
  port: 8080
spring :
#  profiles:
##    active: release
#    include: release
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html

logging:
  level:
    org.hibernate.SQL : debug
    org.hibernate.type: info